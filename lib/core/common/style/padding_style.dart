import 'package:flutter/material.dart';

class AppPadding {
  AppPadding._internal();
  static get styleSmall => const EdgeInsets.all(4);
  static get styleMedium => const EdgeInsets.all(8);
  static get styleLarge => const EdgeInsets.all(16);
}